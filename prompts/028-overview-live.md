# Cursor Prompt 028 — Live Overview Dashboard

Replace overview demo pulse with live pipeline counts.

## Requirements

- Stats: awaiting approval, shopify drafts, safety rejects, trend avg (when available).
- Approval queue preview + top trends from live loaders / engines.
- Keep useful empty-state setup hints when Mongo is empty.

## Verify

typecheck, lint, tests, build.
